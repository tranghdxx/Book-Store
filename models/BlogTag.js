const mongoose = require("mongoose");

const blogCategorySchema = mongoose.Schema({
  tag: {
    type: String,
    required: true,
  },
  createdAt: {
    type: Date,
    default: Date.now,
  },
  updatedAt: {
    type: Date,
    default: Date.now,
  },
});

module.exports = mongoose.model("blog_tags", blogCategorySchema);
